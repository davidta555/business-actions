name: Test and Deploy

on:
  push:
    branches: [main]

jobs:
  tests:
    name: Test
    runs-on: ubuntu-lastest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 14.x
      - run: npm ci
      - run: npm test
